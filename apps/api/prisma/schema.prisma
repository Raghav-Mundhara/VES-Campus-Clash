// This is the Prisma schema file.
// Schema (Player, Session, Step enum) will be added in the next prompt.
// See README.md §4 for the full schema design.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models to be added:
// model Player { ... }
// model Session { ... }
// enum Step { FOLLOWED REGISTERED PLAYING COMPLETED }
